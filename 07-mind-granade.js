const num1 = 5
const num2 = 10

function addValues() {
  console.log(`the sum is: ${num1 + num2}`)
}

addValues()

// all functions in modules wrapped in immedeately envoked function
// if function envoked(called) here can be used anywhere
